// Simple load generator.
{
  "name": "Simple load generator.",
  "config": {
    "priority": 25
  },
  "workers": [
    {
      "instances": 8,
      "actions": [
        { "action": "spin", "uniform" : { "min": "1ms", "max": "16ms" } },
        { "action": "sleep", "uniform" : { "min": "1ms", "max": "16ms" } },
      ]
    }
  ]
}